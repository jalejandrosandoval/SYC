import { InvoicesStatusModel } from './invoices-status-model';

describe('InvoicesStatusModel', () => {
  it('should create an instance', () => {
    expect(new InvoicesStatusModel()).toBeTruthy();
  });
});
